import { useEffect } from 'react';

function Vehicles() {
  //const dispatch = useDispatch();
  //const testBacks = useSelector((state) => state.testBack.list);

  useEffect(() => {
    //getAllTestBacks(dispatch);
  }, []);

  return (
    <section>
      <p> ESTE ES EL VEHICLES </p>
    </section>
  );
}
export default Vehicles;
